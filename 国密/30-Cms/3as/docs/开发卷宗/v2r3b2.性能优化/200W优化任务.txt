    1、3as数据库中用户权限相关信息需要写入redis，具体的数据结构尚未确定，详细方案未定
	（胡志云尽快提供接口   ***）
	
    2、3as、gbu、gbs中存在着遍历redis中设备的操作，需要重新实现，否则导致主线程阻塞
	（同cmu   ***）
	
    3、存在着遍历redis的设备的调试命令，需要调整或者剥离，否则导致主线程阻塞
	（同cmu   ***）
	
    4、gbs中视频源别名和经纬度数据同步Task需要优化
	（作为bug处理）	
	
    5、mysql需要根据业务进行存储引擎变更、分库、分表、读写分离等优化，
	（根据后续测试结果来决定是否完成该工作，3as、dps   ***）
	
    6、gbu和下级国标子域之间经常性的出现断链，需要优化
	（找刘永涛确认  ***）
	
    7、3as中存在大量sql执行时间长的语句，200W测试环境下出现了很多sql执行时间超过1000秒
	（根据后续测试结果来决定是否完成该工作   ***）
	
	
	

3as权限数据写入redis    tas的权限信息写入redis，还涉及初始写入工具

sql性能优化与数据库架构优化

redis循环调用阻塞优化（3as私有域退网、gbu同步TAS中的设备数据）  通过GetDeviceIDList获取id后再循环调用设备具体信息多线程优化

耗时的调试命令 gbs(gbtable)

gbu和下级国标子域之间经常性的出现断链

